name: gharana
description: Family Budget & Money Tracker for Pakistani households.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_sms_inbox: ^1.0.3
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  share_plus: ^9.0.0
  path_provider: ^2.1.2
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
